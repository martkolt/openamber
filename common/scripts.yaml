- id: update_home_thermostat_mode_ui
  mode: restart
  then: []

- id: fetch_analytics_device_id
  then:
    - if:
        condition:
          lambda: return id(analytics_device_id).state.empty() && id(mac_address).has_state() && !id(mac_address).state.empty();
        then:
          - http_request.get:
              url: !lambda |-
                return "http://openamber.nl/api/devices/by-mac/" + id(mac_address).state + "/device-id";
              capture_response: true
              on_response:
                then:
                  - if:
                      condition:
                        lambda: return response->status_code == 200 && body.size() == 6;
                      then:
                        - lambda: |-
                            id(analytics_device_id).publish_state(body);

- id: apply_pid_settings
  then:
    - lambda: |-
        auto call = id(pump_p0_pid_temperature_control).make_call();
        call.set_target_temperature(id(pump_p0_pid_target_delta_t).state);
        call.perform();
    - climate.pid.set_control_parameters:
        id: pump_p0_pid_temperature_control
        kp: !lambda 'return id(pump_p0_pid_kp).state;'
        ki: !lambda 'return id(pump_p0_pid_ki).state;'
        kd: !lambda 'return id(pump_p0_pid_kd).state;'
    - climate.pid.set_control_parameters:
        id: pid_heat_temperature_control
        kp: !lambda 'return id(pid_heat_kp).state;'
        ki: !lambda 'return id(pid_heat_ki).state;'
        kd: !lambda 'return id(pid_heat_kd).state;'
    - lambda: |-
        const float heat_deadband = id(pid_heat_deadband).state;
        id(pid_heat_temperature_control).set_threshold_low(-heat_deadband);
        id(pid_heat_temperature_control).set_threshold_high(heat_deadband);
    - climate.pid.set_control_parameters:
        id: pid_cool_temperature_control
        kp: !lambda 'return id(pid_cool_kp).state;'
        ki: !lambda 'return id(pid_cool_ki).state;'
        kd: !lambda 'return id(pid_cool_kd).state;'
    - lambda: |-
        const float cool_deadband = id(pid_cool_deadband).state;
        id(pid_cool_temperature_control).set_threshold_low(-cool_deadband);
        id(pid_cool_temperature_control).set_threshold_high(cool_deadband);

- id: apply_thermostat_settings
  then:
    - lambda: |-
        // Pas deadband/overrun instellingen toe
        id(climate_controller).set_heat_deadband(id(thermostat_heat_deadband).state);
        id(climate_controller).set_heat_overrun(id(thermostat_heat_overrun).state);
        id(climate_controller).set_cool_deadband(id(thermostat_cool_deadband).state);
        id(climate_controller).set_cool_overrun(id(thermostat_cool_overrun).state);
    - climate.control:
        id: climate_controller
        mode: !lambda |-
          auto mode = id(climate_controller).mode;
          switch (mode) {
            case climate::CLIMATE_MODE_COOL:
              return climate::CLIMATE_MODE_COOL;
            case climate::CLIMATE_MODE_HEAT:
              return climate::CLIMATE_MODE_HEAT;
            default:
              return climate::CLIMATE_MODE_OFF;
          }
        target_temperature_low: !lambda 'return id(thermostat_default_heat_setpoint).state;'
        target_temperature_high: !lambda 'return id(thermostat_default_cool_setpoint).state;'

- id: send_device_info
  then:
    - mqtt.publish_json:
        topic: !lambda 'return id(analytics_topic);'
        qos: 0
        retain: true
        payload: |-
          root["ts"] = id(analytics_batch_timestamp);
          root["fw"] = ESPHOME_PROJECT_VERSION;
          root["mo"] = "${device_model}";

          root["a1"] = id(model_identification_1).state;
          root["a2"] = id(model_identification_2).state;
          root["a3"] = id(model_identification_3).state;

          root["hn"] = id(outside_unit_hardware_item_number).state;

          root["ev"] = id(outside_unit_eeprom_version).state;
          root["e1"] = id(outside_unit_eeprom_version_part1).state;
          root["e2"] = id(outside_unit_eeprom_version_part2).state;

          root["pv"] = id(outside_unit_program_version).state;

- id: send_config_1
  then:
    - mqtt.publish_json:
        topic: !lambda 'return id(analytics_topic);'
        qos: 0
        retain: true
        payload: |-
          root["ts"] = id(analytics_batch_timestamp);
          root["de"] = id(dhw_enabled_switch).state;
          root["lg"] = id(legio_enabled_switch).state;

          root["fa"] = id(frost_protection_stage_1_temp_ta).state;
          root["fb"] = id(frost_protection_stage_2_temp_ta).state;
          root["fc"] = id(frost_protection_stage_2_temp_tui).state;

          root["ca"] = id(compressor_start_delta_heating).state;
          root["cb"] = id(compressor_stop_delta_heating).state;

- id: send_config_2
  then:
    - mqtt.publish_json:
        topic: !lambda 'return id(analytics_topic);'
        qos: 0
        retain: true
        payload: |-
          root["ts"] = id(analytics_batch_timestamp);
          root["h1"] = id(heat_curve_p15).state;
          root["h2"] = id(heat_curve_p10).state;
          root["h3"] = id(heat_curve_p5).state;
          root["h4"] = id(heat_curve_0).state;
          root["h5"] = id(heat_curve_m10).state;

          root["dt"] = id(dhw_setpoint_temperature).state;
          root["dr"] = id(dhw_restart_dhw_delta).state;
          root["dm"] = id(dhw_temperature_threshold_max_compressor_mode).state;

          root["qa"] = id(pid_heat_kp).state;
          root["qb"] = id(pid_heat_ki).state;
          root["qc"] = id(pid_heat_kd).state;

          root["ra"] = id(pid_cool_kp).state;
          root["rb"] = id(pid_cool_ki).state;
          root["rc"] = id(pid_cool_kd).state;

- id: send_config_3
  then:
    - mqtt.publish_json:
        topic: !lambda 'return id(analytics_topic);'
        qos: 0
        retain: true
        payload: |-
          root["ts"] = id(analytics_batch_timestamp);
          root["ba"] = id(dhw_backup_min_avg_rate).state;
          root["bb"] = id(backup_heater_degmin_threshold).state;

          root["ph"] = id(pump_speed_heating_number).state;
          root["pj"] = id(pump_speed_dhw_number).state;
          root["pk"] = id(pump_speed_cooling_number).state;

          root["pi"] = id(pump_interval).state;
          root["pd"] = id(pump_duration).state;

          root["db"] = id(defrost_backup_heater_boost_temperature_sensor).state;

          root["ld"] = id(legio_repeat_days_number).state;
          root["lt"] = id(legio_target_temperature_number).state;

          root["ms"] = id(manual_setpoint).state;

          root["sh"] = id(sg_ready_heating_boost_temperature_number).state;
          root["sj"] = id(sg_ready_dhw_boost_temperature_number).state;

          root["cs"] = id(cooling_setpoint_number).state;
          root["cj"] = id(compressor_start_delta_cooling).state;
          root["ck"] = id(compressor_stop_delta_cooling).state;

- id: send_config_4
  then:
    - mqtt.publish_json:
        topic: !lambda 'return id(analytics_topic);'
        qos: 0
        retain: true
        payload: |-
          root["ts"] = id(analytics_batch_timestamp);

          root["qd"] = id(pid_heat_deadband).state;
          root["rd"] = id(pid_cool_deadband).state;

          root["xa"] = id(climate_controller).mode == climate::CLIMATE_MODE_COOL ? id(climate_controller).target_temperature_high : id(climate_controller).target_temperature_low;
          root["xb"] = id(thermostat_default_heat_setpoint).state;
          root["xc"] = id(thermostat_default_cool_setpoint).state;
          root["xd"] = id(thermostat_heat_deadband).state;
          root["xe"] = id(thermostat_heat_overrun).state;
          root["xf"] = id(thermostat_cool_deadband).state;
          root["xg"] = id(thermostat_cool_overrun).state;

          root["xh"] = id(flow_sensor_calibration).state;
          root["xi"] = id(flow_switch_safety_delay_minutes).state;

          root["xj"] = id(bottomplate_heater_ambient_temperature_start).state;
          root["xk"] = id(bottomplate_heater_ambient_hysteresis_stop).state;

          root["xl"] = id(enter_defrost_temperature).state;
          root["xm"] = id(exit_defrost_temperature).state;
          root["xn"] = id(max_defrost_time).state;

          root["xo"] = id(enter_defrost_temperature_2).state;
          root["xp"] = id(enter_defrost_temperature_3).state;
          root["xq"] = id(enter_defrost_temperature_4).state;

- id: send_modes
  then:
    - mqtt.publish_json:
        topic: !lambda 'return id(analytics_topic);'
        qos: 0
        retain: true
        payload: |-
          root["ts"] = id(analytics_batch_timestamp);
          {
            auto idx = id(heat_compressor_mode).active_index();
            root["mh"] = idx.has_value() ? idx.value() : -1;
          }
          {
            auto idx = id(cool_compressor_mode).active_index();
            root["mc"] = idx.has_value() ? idx.value() : -1;
          }
          {
            auto idx = id(dhw_compressor_mode).active_index();
            root["md"] = idx.has_value() ? idx.value() : -1;
          }
          {
            auto idx = id(dhw_compressor_mode_max).active_index();
            root["mx"] = idx.has_value() ? idx.value() : -1;
          }

          {
            auto idx = id(dhw_pump_start_mode_select).active_index();
            root["mp"] = idx.has_value() ? idx.value() : -1;
          }

          {
            auto idx = id(heat_mode_select).active_index();
            root["me"] = idx.has_value() ? idx.value() : -1;
          }
          {
            auto idx = id(cool_mode_select).active_index();
            root["mf"] = idx.has_value() ? idx.value() : -1;
          }

          {
            auto idx = id(thermostat_mode_select).active_index();
            root["mt"] = idx.has_value() ? idx.value() : -1;
          }
          {
            auto idx = id(sg_ready_mode_select).active_index();
            root["mg"] = idx.has_value() ? idx.value() : -1;
          }

          {
            auto idx = id(backup_heating_mode).active_index();
            root["mb"] = idx.has_value() ? idx.value() : -1;
          }

          {
            auto idx = id(bottomplate_heater_mode).active_index();
            root["xr"] = idx.has_value() ? idx.value() : -1;
          }

          root["wh"] = id(heat_demand_switch).state;
          root["wc"] = id(cool_demand_switch).state;

          root["ws"] = id(dhw_schedule_enabled_switch).state;
          root["wp"] = id(pump_p1_enabled).state;

          root["xs"] = id(emergency_mode_enabled).state;
          root["xt"] = id(service_mode_enabled).state;
          root["xu"] = id(flow_sensor_enabled).state;
          root["xv"] = id(pump_p0_pid_enabled).state;
          root["xw"] = id(pump_p0_pid_target_delta_t).state;
          root["xx"] = id(pump_p0_pid_min_pwm).state;
          root["xy"] = id(pump_p0_pid_max_pwm).state;
          root["xz"] = id(pump_p0_pid_kp).state;
          root["za"] = id(pump_p0_pid_ki).state;
          root["zb"] = id(pump_p0_pid_kd).state;
          root["zc"] = id(pump_p0_pid_defrost_pwm).state;

- id: send_telemetry_1
  then:
    - mqtt.publish_json:
        topic: !lambda 'return id(analytics_topic);'
        qos: 0
        retain: true
        payload: |-
          root["ts"] = id(analytics_batch_timestamp);
          root["th"] = id(heat_demand_active_sensor).state;
          root["tc"] = id(cool_demand_active_sensor).state;
          root["td"] = id(dhw_demand_active_sensor).state;

          root["te"] = id(current_setpoint).state;
          root["tf"] = id(room_temperature).state;
          root["tg"] = id(temperature_outside_ta).state;

          root["ti"] = id(dhw_temperature_tw_sensor).state;
          root["tj"] = id(heat_cool_temperature_tc).state;

          root["tk"] = id(outlet_temperature_tuo).state;
          root["tl"] = id(inlet_temperature_tui).state;

- id: send_telemetry_2
  then:
    - mqtt.publish_json:
        topic: !lambda 'return id(analytics_topic);'
        qos: 0
        retain: true
        payload: |-
          root["ts"] = id(analytics_batch_timestamp);
          root["ua"] = id(condensate_temperature_tup).state;
          root["ub"] = id(evaporation_temperature_tp).state;

          root["uc"] = id(suction_temperature_ts).state;
          root["ud"] = id(discharge_temperature_td).state;

          root["ue"] = id(suction_pressure_ps).state;
          root["uf"] = id(discharge_pressure_pd).state;

          root["ug"] = id(fan_rpm_sensor).state;
          root["uh"] = id(power_compressor).state;

          root["ui"] = id(current_compressor_frequency).state;
          root["uj"] = id(compressor_setpoint_frequency).state;

- id: send_telemetry_3
  then:
    - mqtt.publish_json:
        topic: !lambda 'return id(analytics_topic);'
        qos: 0
        retain: true
        payload: |-
          root["ts"] = id(analytics_batch_timestamp);
          root["va"] = id(backup_heater_active_sensor).state;
          root["vb"] = id(internal_pump_active).state;
          root["vc"] = id(defrost_active_sensor).state;

          root["vd"] = id(backup_heater_degmin_current_sensor).state;
          root["ve"] = id(dhw_backup_current_avg_rate_sensor).state;

          root["vf"] = id(state_machine_state_dhw).state;
          root["vg"] = id(state_machine_state_heat_cool).state;
          root["vh"] = id(state_machine_state_main).state;

          root["vi"] = id(sensor_uptime).state;

          root["vj"] = id(current_dhw_setpoint_sensor).state;
          root["vk"] = id(dhw_legionella_run_active_sensor).state;

          root["vl"] = id(three_way_valve_active_sensor).state;
          root["vm"] = id(dhw_schedule_active_sensor).state;

          root["vn"] = id(pump_p1_relay_switch).state;
          root["vo"] = id(dhw_pump_relay_switch).state;

          root["vp"] = id(expansion_valve_position).state;
          root["vq"] = id(pump_p0_current_pwm_sensor).state;

          {
            auto idx = id(working_mode_switch).active_index();
            root["vr"] = idx.has_value() ? idx.value() : -1;
          }

- id: send_telemetry_4
  then:
    - mqtt.publish_json:
        topic: !lambda 'return id(analytics_topic);'
        qos: 0
        retain: true
        payload: |-
          root["ts"] = id(analytics_batch_timestamp);
          root["ya"] = id(wifi_signal_db).state;
          root["yb"] = id(system_heap_free).state;
          root["yc"] = id(system_heap_fragmentation).state;
          root["yd"] = id(system_heap_max_block).state;
          root["ye"] = id(water_flow_rate).state;

- id: schedule_send_errors_realtime
  mode: restart
  then:
    - delay: 200ms
    - script.execute: send_errors_realtime

- id: send_errors_realtime
  then:
    - if:
        condition:
          - switch.is_on: analytics_enabled_switch
        then:
          - mqtt.publish_json:
              topic: !lambda 'return id(analytics_topic);'
              qos: 0
              retain: true
              payload: |-
                root["ts"] = id(my_time).timestamp_now();
                root["e2119"] = (uint16_t) id(error_register_2119_raw).state;
                root["e2120"] = (uint16_t) id(error_register_2120_raw).state;
                root["e2121"] = (uint16_t) id(error_register_2121_raw).state;

- id: send_analytics
  then:
    - lambda: |-
        id(analytics_batch_timestamp) = id(my_time).timestamp_now();
    - script.execute: send_device_info
    - delay: 150ms
    - script.execute: send_config_1
    - delay: 150ms
    - script.execute: send_config_2
    - delay: 150ms
    - script.execute: send_config_3
    - delay: 150ms
    - script.execute: send_config_4
    - delay: 150ms
    - script.execute: send_modes
    - delay: 150ms
    - script.execute: send_telemetry_1
    - delay: 150ms
    - script.execute: send_telemetry_2
    - delay: 150ms
    - script.execute: send_telemetry_3
    - delay: 150ms
    - script.execute: send_telemetry_4
    - delay: 150ms
    - script.execute: fetch_analytics_device_id

- id: dump_eeprom_parameters_ui_disable
  then: []

- id: dump_eeprom_parameters_ui_enable
  then: []

- id: dump_eeprom_parameters
  mode: single
  then:
    - script.execute: dump_eeprom_parameters_ui_disable
    - if:
        condition:
          lambda: 'return id(modbus_outside_is_online);'
        then:
          - if:
              condition:
                lambda: 'return id(mqtt_client).is_connected();'
              then:
                - lambda: |-
                    static bool in_progress = false;
                    static int request_sequence = 0;
                    static int pending_chunks = 0;
                    if (in_progress) {
                      ESP_LOGW("amber", "EEPROM dump overgeslagen: er loopt al een dump");
                      id(dump_eeprom_parameters_ui_enable).execute();
                      return;
                    }
                    in_progress = true;
                    const int request_seq = ++request_sequence;
                    pending_chunks = 5;
                    const int64_t request_ts = id(my_time).timestamp_now();
                    auto complete_chunk = [request_seq](bool failed) {
                      if (request_seq != request_sequence) {
                        return;
                      }
                      if (failed) {
                        ESP_LOGE("amber", "EEPROM dump chunk mislukt");
                      }
                      if (--pending_chunks <= 0) {
                        pending_chunks = 0;
                        in_progress = false;
                        id(dump_eeprom_parameters_ui_enable).execute();
                      }
                    };
                    auto queue_chunk = [request_seq, request_ts, complete_chunk](uint16_t start_address, uint16_t register_count, uint8_t chunk_index) {
                      auto command = esphome::modbus_controller::ModbusCommandItem::create_read_command(
                        id(modbus_outside_unit),
                        esphome::modbus::EntityType::HOLDING,
                        start_address,
                        register_count,
                            [request_seq, request_ts, start_address, register_count, chunk_index, complete_chunk](
                            esphome::modbus::EntityType register_type,
                            uint16_t response_start_address,
                            std::span<const uint8_t> data) {
                          (void) register_type;
                          (void) response_start_address;
                          if (request_seq != request_sequence) {
                            return;
                          }
                          const size_t expected_bytes = register_count * 2;
                          if (data.size() < expected_bytes) {
                            ESP_LOGE("amber", "EEPROM dump chunk %u mislukt: verwachtte %u bytes, kreeg %u",
                                     chunk_index, expected_bytes, data.size());
                            complete_chunk(true);
                            return;
                          }

                          std::string payload = "{\"ts\":";
                          payload += std::to_string(request_ts);
                          payload += ",\"start\":";
                          payload += std::to_string(start_address + 1);
                          payload += ",\"count\":";
                          payload += std::to_string(register_count);
                          payload += ",\"values\":[";
                          for (size_t i = 0; i < register_count; i++) {
                            const size_t offset = i * 2;
                            const uint16_t value = (static_cast<uint16_t>(data[offset]) << 8) |
                                                   static_cast<uint16_t>(data[offset + 1]);
                            if (i > 0) payload += ",";
                            payload += std::to_string(value);
                          }
                          payload += "]}";

                          const std::string topic = id(analytics_topic) + "/eeprom_dump/chunk/" + std::to_string(chunk_index);
                          if (!id(mqtt_client).publish(topic, payload, 0, false)) {
                            ESP_LOGE("amber", "EEPROM dump chunk %u kon niet naar MQTT worden gepubliceerd", chunk_index);
                            complete_chunk(true);
                            return;
                          }

                          ESP_LOGI("amber", "EEPROM dump chunk %u gepubliceerd (%u-%u)", chunk_index, start_address + 1,
                                   start_address + register_count);
                          complete_chunk(false);
                        });
                      id(modbus_outside_unit)->queue_command(command);
                    };

                    queue_chunk(2999, 125, 0);
                    queue_chunk(3124, 125, 1);
                    queue_chunk(3249, 125, 2);
                    queue_chunk(3374, 125, 3);
                    queue_chunk(3499, 12, 4);
                - logger.log: "EEPROM dump gestart (3000 t/m 3511)"
              else:
                - logger.log: "EEPROM dump geannuleerd: MQTT niet verbonden"
                - script.execute: dump_eeprom_parameters_ui_enable
        else:
          - logger.log: "EEPROM dump geannuleerd: buitenunit Modbus offline"
          - script.execute: dump_eeprom_parameters_ui_enable
