- platform: template
  name: Thermisch vermogen
  id: thermal_power
  accuracy_decimals: 0
  device_class: power
  state_class: measurement
  unit_of_measurement: W
  entity_category: diagnostic
  lambda: |-
      if (!id(flow_sensor_enabled).state)
      {
        return 0.0;
      }

      float qv = id(water_flow_rate).state / 60 / 1000;
      float dT = id(heat_exchanger_delta_t).state;
      float pw = qv * dT * 998 * 4190;
      return pw;

- platform: pid
  name: "PID Controller Verwarmen"
  climate_id: pid_heat_temperature_control
  type: HEAT
  disabled_by_default: True
  entity_category: diagnostic
  web_server: 
    sorting_group_id: diagnostic

- platform: pid
  name: "PID Controller Koelen"
  climate_id: pid_cool_temperature_control
  type: COOL
  disabled_by_default: True
  entity_category: diagnostic
  web_server: 
    sorting_group_id: diagnostic

- platform: template
  id: backup_heater_degmin_current_sensor
  name: "Backup heater degree⁄min current"
  unit_of_measurement: "°C·min"
  accuracy_decimals: 1
  state_class: measurement
  entity_category: diagnostic
  web_server: 
    sorting_group_id: diagnostic

- platform: template
  id: dhw_backup_current_avg_rate_sensor
  name: "DHW verwarmsnelheid"
  unit_of_measurement: "°C/min"
  accuracy_decimals: 2
  state_class: measurement
  entity_category: diagnostic
  web_server: 
    sorting_group_id: diagnostic
  filters:
    - sliding_window_moving_average:
        window_size: 60
        send_every: 1

- platform: template
  id: power_compressor
  name: "Stroomverbruik compressor"
  device_class: power
  state_class: measurement
  unit_of_measurement: "W"
  lambda: |-
    return id(voltage_outside_unit).state * id(current_outside_unit).state;
  web_server: 
    sorting_group_id: diagnostic

- platform: template
  id: cop_thermal_power_heating
  internal: true
  device_class: power
  state_class: measurement
  unit_of_measurement: "W"
  accuracy_decimals: 0
  update_interval: 30s
  lambda: |-
    if (!id(flow_sensor_enabled).state) return 0.0f;
    if (id(current_compressor_frequency).state <= 0 || id(defrost_active_sensor).state) return 0.0f;
    if (id(dhw_active).state) return 0.0f;
    if (!id(heat_demand_active_sensor).state) return 0.0f;
    return fabsf(id(thermal_power).state);

- platform: template
  id: cop_electrical_power_heating
  internal: true
  device_class: power
  state_class: measurement
  unit_of_measurement: "W"
  accuracy_decimals: 0
  update_interval: 30s
  lambda: |-
    if (!id(flow_sensor_enabled).state) return 0.0f;
    if (id(current_compressor_frequency).state <= 0 || id(defrost_active_sensor).state) return 0.0f;
    if (id(dhw_active).state) return 0.0f;
    if (!id(heat_demand_active_sensor).state) return 0.0f;
    return id(power_compressor).state;

# Cooling-only power samples for EER/SEER energy tracking.
- platform: template
  id: cop_thermal_power_cooling
  internal: true
  device_class: power
  state_class: measurement
  unit_of_measurement: "W"
  accuracy_decimals: 0
  update_interval: 30s
  lambda: |-
    if (!id(flow_sensor_enabled).state) return 0.0f;
    if (id(current_compressor_frequency).state <= 0 || id(defrost_active_sensor).state) return 0.0f;
    if (id(dhw_active).state) return 0.0f;
    if (!id(cool_demand_active_sensor).state) return 0.0f;
    return fabsf(id(thermal_power).state);

- platform: template
  id: cop_electrical_power_cooling
  internal: true
  device_class: power
  state_class: measurement
  unit_of_measurement: "W"
  accuracy_decimals: 0
  update_interval: 30s
  lambda: |-
    if (!id(flow_sensor_enabled).state) return 0.0f;
    if (id(current_compressor_frequency).state <= 0 || id(defrost_active_sensor).state) return 0.0f;
    if (id(dhw_active).state) return 0.0f;
    if (!id(cool_demand_active_sensor).state) return 0.0f;
    return id(power_compressor).state;

# DHW-only power samples for COP energy tracking.
- platform: template
  id: cop_thermal_power_dhw
  internal: true
  device_class: power
  state_class: measurement
  unit_of_measurement: "W"
  accuracy_decimals: 0
  update_interval: 30s
  lambda: |-
    if (!id(flow_sensor_enabled).state) return 0.0f;
    if (id(current_compressor_frequency).state <= 0 || id(defrost_active_sensor).state) return 0.0f;
    if (!id(dhw_active).state) return 0.0f;
    return fabsf(id(thermal_power).state);

- platform: template
  id: cop_electrical_power_dhw
  internal: true
  device_class: power
  state_class: measurement
  unit_of_measurement: "W"
  accuracy_decimals: 0
  update_interval: 30s
  lambda: |-
    if (!id(flow_sensor_enabled).state) return 0.0f;
    if (id(current_compressor_frequency).state <= 0 || id(defrost_active_sensor).state) return 0.0f;
    if (!id(dhw_active).state) return 0.0f;
    return id(power_compressor).state;

# Per-mode energy buckets (kWh). Reset by the time: component.
- platform: integration
  id: thermal_energy_hour_heating
  internal: true
  restore: false
  sensor: cop_thermal_power_heating
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: electrical_energy_hour_heating
  internal: true
  restore: false
  sensor: cop_electrical_power_heating
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: thermal_energy_day_heating
  internal: true
  restore: false
  sensor: cop_thermal_power_heating
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: electrical_energy_day_heating
  internal: true
  restore: false
  sensor: cop_electrical_power_heating
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: thermal_energy_hour_cooling
  internal: true
  restore: false
  sensor: cop_thermal_power_cooling
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: electrical_energy_hour_cooling
  internal: true
  restore: false
  sensor: cop_electrical_power_cooling
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: thermal_energy_day_cooling
  internal: true
  restore: false
  sensor: cop_thermal_power_cooling
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: electrical_energy_day_cooling
  internal: true
  restore: false
  sensor: cop_electrical_power_cooling
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: thermal_energy_hour_dhw
  internal: true
  restore: false
  sensor: cop_thermal_power_dhw
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: electrical_energy_hour_dhw
  internal: true
  restore: false
  sensor: cop_electrical_power_dhw
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: thermal_energy_day_dhw
  internal: true
  restore: false
  sensor: cop_thermal_power_dhw
  time_unit: h
  filters:
    - multiply: 0.001

- platform: integration
  id: electrical_energy_day_dhw
  internal: true
  restore: false
  sensor: cop_electrical_power_dhw
  time_unit: h
  filters:
    - multiply: 0.001

# COP / EER / SEER per period = thermal energy / electrical energy.
- platform: template
  name: "COP verwarmen (uur)"
  id: cop_hour_heating
  accuracy_decimals: 2
  state_class: measurement
  update_interval: 60s
  lambda: |-
    float e = id(electrical_energy_hour_heating).state;
    float t = id(thermal_energy_hour_heating).state;
    if (std::isnan(e) || std::isnan(t) || e < 0.001f) return NAN;
    return t / e;
  web_server: 
    sorting_group_id: diagnostic

- platform: template
  name: "COP verwarmen (dag)"
  id: cop_day_heating
  accuracy_decimals: 2
  state_class: measurement
  update_interval: 60s
  lambda: |-
    float e = id(electrical_energy_day_heating).state;
    float t = id(thermal_energy_day_heating).state;
    if (std::isnan(e) || std::isnan(t) || e < 0.001f) return NAN;
    return t / e;
  web_server: 
    sorting_group_id: diagnostic


- platform: template
  name: "EER koelen (uur)"
  id: eer_hour_cooling
  accuracy_decimals: 2
  state_class: measurement
  update_interval: 60s
  lambda: |-
    float e = id(electrical_energy_hour_cooling).state;
    float t = id(thermal_energy_hour_cooling).state;
    if (std::isnan(e) || std::isnan(t) || e < 0.001f) return NAN;
    return t / e;
  web_server: 
    sorting_group_id: diagnostic

- platform: template
  name: "EER koelen (dag)"
  id: eer_day_cooling
  accuracy_decimals: 2
  state_class: measurement
  update_interval: 60s
  lambda: |-
    float e = id(electrical_energy_day_cooling).state;
    float t = id(thermal_energy_day_cooling).state;
    if (std::isnan(e) || std::isnan(t) || e < 0.001f) return NAN;
    return t / e;
  web_server: 
    sorting_group_id: diagnostic


- platform: template
  name: "COP tapwater (uur)"
  id: cop_hour_dhw
  accuracy_decimals: 2
  state_class: measurement
  update_interval: 60s
  lambda: |-
    float e = id(electrical_energy_hour_dhw).state;
    float t = id(thermal_energy_hour_dhw).state;
    if (std::isnan(e) || std::isnan(t) || e < 0.001f) return NAN;
    return t / e;
  web_server: 
    sorting_group_id: diagnostic

- platform: template
  name: "COP tapwater (dag)"
  id: cop_day_dhw
  accuracy_decimals: 2
  state_class: measurement
  update_interval: 60s
  lambda: |-
    float e = id(electrical_energy_day_dhw).state;
    float t = id(thermal_energy_day_dhw).state;
    if (std::isnan(e) || std::isnan(t) || e < 0.001f) return NAN;
    return t / e;
  web_server: 
    sorting_group_id: diagnostic

          
- platform: uptime
  name: "Uptime"
  id: sensor_uptime
  disabled_by_default: true
  update_interval: 60s
  device_class: duration
  unit_of_measurement: s
  state_class: total_increasing
  entity_category: diagnostic
  web_server: 
    sorting_group_id: diagnostic

- platform: wifi_signal
  name: WiFi signal dB
  disabled_by_default: true
  id: wifi_signal_db
  update_interval: 20s
  device_class: signal_strength
  entity_category: diagnostic
  web_server: 
    sorting_group_id: diagnostic

- platform: debug
  free:
    name: "Heap Free"
    id: system_heap_free
    disabled_by_default: true
  fragmentation:
    name: "Heap Fragmentation"
    id: system_heap_fragmentation
    disabled_by_default: true
  block:
    name: "Heap Max Block"
    id: system_heap_max_block
    disabled_by_default: true

- platform: template
  id: temperature_outside_ta_avg_1h
  name: "Buiten temperatuur (1h gemiddelde)"
  internal: true
  unit_of_measurement: "°C"
  accuracy_decimals: 1
  update_interval: 60s
  lambda: |-
    return id(temperature_outside_ta).state;
  filters:
    - median:
        window_size: 5
        send_every: 1
    - sliding_window_moving_average:
        window_size: 60
        send_every: 1

- platform: template
  id: current_setpoint
  name: "Huidig setpoint"
  unit_of_measurement: "°C"
  device_class: temperature
  state_class: measurement
  accuracy_decimals: 0
  lambda: |-
    float setpoint = 0.0;
    if (id(cool_demand_active_sensor).state)
    {
      if (id(cool_mode_select).current_option() == "Extern setpoint")
      {
        return id(manual_setpoint).state;
      }
      else
      {
        return id(cooling_setpoint_number).state;
      }
    }

    if (id(heat_mode_select).current_option() == "Stooklijn")
    {
      setpoint = id(heat_curve_calculated_setpoint).state;
    }
    else
    {
      setpoint = id(manual_setpoint).state;
    }

    if(id(sg_ready_boost_mode_active_sensor).state || id(sg_ready_max_boost_mode_active_sensor).state)
    {
      setpoint += id(sg_ready_heating_boost_temperature_number).state;
    }

    return setpoint;
  on_value:
    then:
      - lambda: |-
          auto climate = id(cool_demand_active_sensor).state ? id(pid_cool_temperature_control) : id(pid_heat_temperature_control);
          auto call = climate->make_call();
          call.set_target_temperature(x);
          call.perform();
  web_server: 
    sorting_group_id: overview

- platform: template
  id: current_dhw_setpoint_sensor
  name: "Huidig tapwater setpoint"
  unit_of_measurement: "°C"
  device_class: temperature
  state_class: measurement
  accuracy_decimals: 0
  lambda: |-
    float setpoint = 0.0;
    if (id(dhw_legionella_run_active_sensor).state)
    {
      setpoint = id(legio_target_temperature_number).state;
    }
    else
    {
      setpoint = id(dhw_setpoint_temperature).state;
    }

    if(id(sg_ready_boost_mode_active_sensor).state || id(sg_ready_max_boost_mode_active_sensor).state)
    {
      setpoint += id(sg_ready_dhw_boost_temperature_number).state;
    }

    return setpoint;
  web_server: 
    sorting_group_id: overview

- platform: template
  id: heat_curve_calculated_setpoint
  name: "Stooklijn berekend setpoint"
  internal: True
  unit_of_measurement: "°C"
  accuracy_decimals: 0
  update_interval: 15s
  lambda: |-
    const float Ta = id(temperature_outside_ta_avg_1h).state;

    const float tc15  = id(heat_curve_p15).state;
    const float tc10  = id(heat_curve_p10).state;
    const float tc5   = id(heat_curve_p5).state;
    const float tc0   = id(heat_curve_0).state;
    const float tc_m10 = id(heat_curve_m10).state;
    auto lerp = [](float x, float x0, float y0, float x1, float y1) -> float {
      const float t = (x - x0) / (x1 - x0);
      return y0 + t * (y1 - y0);
    };

    float tc;
    if (Ta >= 15.0f)       tc = tc15;
    else if (Ta >= 10.0f)  tc = lerp(Ta, 15.0f, tc15, 10.0f, tc10);
    else if (Ta >= 5.0f)   tc = lerp(Ta, 10.0f, tc10, 5.0f, tc5);
    else if (Ta >= 0.0f)   tc = lerp(Ta, 5.0f, tc5, 0.0f, tc0);
    else if (Ta >= -10.0f) tc = lerp(Ta, 0.0f, tc0, -10.0f, tc_m10);
    else                   tc = tc_m10;

    return tc;
  filters:
    - round_to_multiple_of: 1

- platform: template
  id: heat_exchanger_delta_t
  name: "∆T Warmtewisselaar"
  device_class: temperature_delta
  state_class: measurement
  unit_of_measurement: °C
  update_interval: never
  accuracy_decimals: 1
  lambda: |-
    return id(outlet_temperature_tuo).state - id(inlet_temperature_tui).state;
  web_server: 
    sorting_group_id: temperature_sensors

- platform: modbus_controller
  modbus_controller_id: modbus_inside_unit
  id: room_temperature
  internal: True # Already readable through thermostat
  name: "Ruimte temperatuur (Tr)"
  address: 1199
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  register_count: 16 # 1199 - 1214
  filters:
    - offset: -3000
    - multiply: 0.01
    - lambda: return x + id(tr_offset).state;

- platform: modbus_controller
  modbus_controller_id: modbus_inside_unit
  id: dhw_temperature_tw_sensor
  name: "Tapwatertemperatuur (Tw)"
  address: 1200
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: overview
  filters:
    - offset: -3000
    - multiply: 0.01
    - lambda: return x + id(tw_offset).state;

- platform: modbus_controller
  modbus_controller_id: modbus_inside_unit
  id: heat_cool_temperature_tc
  name: "CV-aanvoertemperatuur (Tc)"
  address: 1201
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: overview
  filters:
    - offset: -3000
    - multiply: 0.01
    - lambda: return x + id(tc_offset).state;
    - throttle: 30s # Throttle because PID loop depends on this

- platform: template
  id: heat_cool_control_temperature
  name: "Regeltemperatuur"
  unit_of_measurement: "°C"
  device_class: temperature
  state_class: measurement
  accuracy_decimals: 1
  update_interval: 30s
  lambda: |-
    auto selected_source = id(heat_cool_control_temperature_source_select).active_index();
    int source = (selected_source.has_value() && selected_source.value() == 1) ? 1 : 0;
    if (source == 1)
    {
      return id(heat_cool_temperature_tv1).state;
    }
    return id(heat_cool_temperature_tc).state;

- platform: modbus_controller
  modbus_controller_id: modbus_inside_unit
  id: outlet_temperature_tuo
  name: "CV-aanvoertemperatuur (Tuo)"
  address: 1202
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01
    - lambda: return x + id(tuo_offset).state;
  on_value: 
    then:
      - component.update: heat_exchanger_delta_t

- platform: modbus_controller
  modbus_controller_id: modbus_inside_unit
  id: inlet_temperature_tui
  name: "CV-retourtemperatuur (Tui)"
  address: 1203
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01
    - lambda: return x + id(tui_offset).state;
  on_raw_value: 
    then:
      - number.set: 
          id: outlet_temperature_outside
          value: !lambda "return x;"
  on_value: 
    then:
      - component.update: heat_exchanger_delta_t

- platform: modbus_controller
  modbus_controller_id: modbus_inside_unit
  id: condensate_temperature_tup
  name: "Condensatietemperatuur (Tup)"
  disabled_by_default: True
  address: 1204
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01

- platform: modbus_controller
  modbus_controller_id: modbus_inside_unit
  id: heat_cool_temperature_tv1
  name: "Cv-aanvoertemperatuur zone 1 (Tv1)"
  address: 1207
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01

- platform: modbus_controller
  modbus_controller_id: modbus_inside_unit
  name: "Cv-aanvoertemperatuur zone 2 (Tv2)"
  address: 1208
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01

- platform: modbus_controller
  modbus_controller_id: modbus_inside_unit
  id: pump_p0_current_pwm_sensor
  name: "Pomp P0 PWM"
  address: 1303
  register_type: holding
  value_type: S_WORD
  unit_of_measurement: "%"
  state_class: "measurement"
  filters:
    - lambda: |-
        return ((x * -1) + 1000) / 10;
  web_server: 
    sorting_group_id: overview
  on_value: 
    then:
      - lambda: 
          id(modbus_inside_is_online) = true;

# Outside unit
- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: voltage_outside_unit
  name: "Voltage buitenunit"
  disabled_by_default: True
  address: 2100
  register_type: holding
  value_type: S_WORD
  device_class: "voltage"
  unit_of_measurement: "V"
  accuracy_decimals: 0
  state_class: "measurement"
  register_count: 33 # 2100-2132
  entity_category: diagnostic
  web_server: 
    sorting_group_id: diagnostic

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: current_outside_unit
  name: "Stroom buitenunit"
  disabled_by_default: True
  address: 2101
  register_type: holding
  value_type: S_WORD
  device_class: "current"
  accuracy_decimals: 1
  unit_of_measurement: "A"
  state_class: "measurement"
  entity_category: diagnostic
  filters:
    - multiply: 0.1
  web_server: 
    sorting_group_id: diagnostic

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: compressor_setpoint_frequency
  internal: True
  address: 2102
  register_type: holding
  value_type: S_WORD
  device_class: frequency
  accuracy_decimals: 0
  state_class: measurement
  unit_of_measurement: Hz

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: current_compressor_frequency
  name: "Compressor frequentie"
  address: 2103
  register_type: holding
  value_type: S_WORD
  device_class: frequency
  accuracy_decimals: 0
  state_class: measurement
  unit_of_measurement: Hz
  web_server: 
    sorting_group_id: overview

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  name: "Fan RPM setpoint"
  internal: True
  address: 2104
  register_type: holding
  value_type: S_WORD
  accuracy_decimals: 0
  state_class: measurement
  unit_of_measurement: rpm

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  name: "Fan RPM"
  id: fan_rpm_sensor
  address: 2105
  register_type: holding
  value_type: S_WORD
  accuracy_decimals: 0
  state_class: measurement
  unit_of_measurement: rpm
  entity_category: diagnostic
  web_server: 
    sorting_group_id: diagnostic

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: expansion_valve_position
  name: "Expansieklep"
  address: 2107
  register_type: holding
  value_type: S_WORD
  state_class: "measurement"
  entity_category: diagnostic
  web_server: 
    sorting_group_id: diagnostic

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: temperature_outside_ta
  name: "Buitentemperatuur (Ta)"
  address: 2110
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: evaporation_temperature_tp
  name: "Verdampingstemperatuur (Tp)"
  disabled_by_default: True
  address: 2111
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: discharge_temperature_td
  name: "Persgastemperatuur (Td)"
  disabled_by_default: True
  address: 2112
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C   
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: suction_temperature_ts
  name: "Zuigastemperatuur (Ts)"
  disabled_by_default: True
  address: 2113
  register_type: holding
  value_type: S_WORD
  device_class: temperature
  state_class: measurement
  unit_of_measurement: °C     
  accuracy_decimals: 1
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: suction_pressure_ps
  name: "Zuiggasdruk (Ps)"
  disabled_by_default: True
  address: 2116
  register_type: holding
  value_type: S_WORD
  device_class: pressure
  accuracy_decimals: 1
  state_class: measurement
  unit_of_measurement: bar
  entity_category: diagnostic
  filters:
    - multiply: 0.1

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: discharge_pressure_pd
  name: "Persgasdruk (Pd)"
  disabled_by_default: True
  address: 2117
  register_type: holding
  value_type: S_WORD
  device_class: pressure
  accuracy_decimals: 1
  state_class: measurement
  unit_of_measurement: bar
  entity_category: diagnostic
  filters:
    - multiply: 0.1

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: error_register_2119_raw
  internal: True
  address: 2119
  register_type: holding
  value_type: U_WORD
  on_value:
    then:
      - script.execute: schedule_send_errors_realtime

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: error_register_2120_raw
  internal: True
  address: 2120
  register_type: holding
  value_type: U_WORD
  on_value:
    then:
      - script.execute: schedule_send_errors_realtime

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: error_register_2121_raw
  internal: True
  address: 2121
  register_type: holding
  value_type: U_WORD
  on_value:
    then:
      - script.execute: schedule_send_errors_realtime

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: outside_unit_program_version
  internal: True
  address: 2122
  register_type: holding
  value_type: S_WORD

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: outside_unit_eeprom_version
  name: "EEPROM"
  address: 2123
  register_type: holding
  value_type: S_WORD
  entity_category: diagnostic

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  name: "Hardware item nummer"
  id: outside_unit_hardware_item_number
  disabled_by_default: true
  address: 2127
  register_type: holding
  value_type: S_WORD
  entity_category: diagnostic

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  name: "Condensing Temperature"
  disabled_by_default: True
  register_type: holding
  address: 2131
  unit_of_measurement: "°C"
  device_class: "temperature"
  state_class: "measurement"
  accuracy_decimals: 2
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  name: "Evaporating Temperature"
  disabled_by_default: True
  register_type: holding
  address: 2132
  unit_of_measurement: "°C"
  device_class: "temperature"
  state_class: "measurement"
  accuracy_decimals: 2
  web_server: 
    sorting_group_id: temperature_sensors
  filters:
    - offset: -3000
    - multiply: 0.01

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: model_identification_1
  skip_updates: 10
  internal: True
  address: 3257
  register_type: holding
  value_type: S_WORD

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: model_identification_2
  skip_updates: 10
  internal: True
  address: 3300
  register_type: holding
  value_type: S_WORD

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: model_identification_3
  skip_updates: 10
  internal: True
  address: 3343
  register_type: holding
  value_type: S_WORD

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: outside_unit_eeprom_version_part2
  internal: True
  skip_updates: 10
  address: 3509
  register_type: holding
  value_type: S_WORD

- platform: modbus_controller
  modbus_controller_id: modbus_outside_unit
  id: outside_unit_eeprom_version_part1
  internal: True
  skip_updates: 10
  address: 3510
  register_type: holding
  value_type: S_WORD
